## Welcome to **WebTerminal** <small>v{{version}}</small>

You are logged in as <span class="prompt-user">{{user}}</span>@<span class="prompt-path">{{host}}</span>

### Quick start:
- <code>help</code> — список команд
- <code>ls</code> — просмотр директории  
- <code>edit readme.txt</code> — открыть файл
- <code>theme</code> — сменить тему
- <code>matrix</code> — эффект Матрицы (Esc для выхода)

> 💡 Все данные сохраняются в вашем браузере через `localStorage`.